export const getObjectInfo = (contentsStr, contentsObj, size) => ({
  contentsStr, // (contenido del archivo leído en formato string)
  contentsObj, // (contenido del archivo leído en formato objeto),
  size, // (tamaño en bytes del archivo)
});
